/**
 * 云端模块，提供存取数据库的方法
 * @module cloud
 */

import * as dataTypes from './types'
import Taro from '@tarojs/taro'

const UserDB = {
    _userId: -1,
    /**
     * 获取当前用户的ID
     * @returns {number} 用户ID
     */
    getUserId() {
        
    }
}

export default {
    UserDB
}